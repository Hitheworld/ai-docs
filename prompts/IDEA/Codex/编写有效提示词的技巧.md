# 包含验证的提示词

    编写一个函数来对列表进行排序。包含测试用例以验证其能否处理空列表、已排序列表、反向排序列表以及包含重复项的列表。运行测试以确认它们都能通过。

# 将复杂任务拆分为小步骤
    步骤 1：创建包含电子邮件和密码字段的用户模型
    步骤 2：添加注册端点
    步骤 3：添加使用 JWT 的登录端点
    步骤 4：添加密码重置功能
    在进入步骤 2 之前，请先告知我步骤 1 已完成。

# 提供上下文
    修复了 src/auth.py 中的一个漏洞，该漏洞导致用户无法使用密码中的特殊字符进行登录。我附上了错误消息的截图以及相关的代码段。

# 明确输出格式
    # 指定代码风格
    编写一个函数来计算斐波那契数列。使用 TypeScript 语言，包含 JSDoc 注释，并遵循函数式编程原则。

    # 指定输出结构
    解释代码库结构。请按照以下顺序组织您的回答：1）概述 2）关键目录 3）主要入口点

    
    设计并实现一个具有创建、读取、更新和删除操作的 REST API，用于博客系统。

    
    审查这段代码，查找可能存在的安全漏洞，并提出改进建议。

# 限制修改范围
    # 只读模式
    解释一下这个功能的作用，不要做任何改动。

    # 限定文件范围
    仅在认证模块中添加日志记录功能，不要改动其他文件。

    # 逐步确认
    在你实施这些改动之前，请先告知我所提议的改动内容。

